This example illustrates how to set up the Arbitrary Lagrangian-Eulerian (ALE) framework in Neko to simulate a simple oscillating cylinder.

To generate the mesh, execut `generate_mesh.sh` in the mesh folder.

