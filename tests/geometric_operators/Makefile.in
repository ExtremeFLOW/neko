USEMPI=NO
ifneq ("$(wildcard @PFUNIT_DIR@/include/PFUNIT.mk)", "")
include @PFUNIT_DIR@/include/PFUNIT.mk
endif
FFLAGS += $(PFUNIT_EXTRA_FFLAGS) -I@top_builddir@/src
FC = @FC@

%.o : %.F90
	$(FC) -c $(FFLAGS) $<


check: distance_test


distance_suite_TESTS := distance.pf
distance_suite_OTHER_LIBRARIES = -L@top_builddir@/src -lneko
$(eval $(call make_pfunit_test,distance_test))


clean:
	$(RM) *.o *.mod *.a *.inc *.F90 distance_test



all:
html:
install:
distdir:
