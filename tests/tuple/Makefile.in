include @PFUNIT_DIR@/include/PFUNIT.mk
FFLAGS += $(PFUNIT_EXTRA_FFLAGS) -I@top_builddir@/src
FC = @FC@

%.o : %.F90
	$(FC) -c $(FFLAGS) $<


check: tuple_test


tuple_test_TESTS := tuple.pf
tuple_test_OTHER_LIBRARIES = -L@top_builddir@/src -lneko
$(eval $(call make_pfunit_test,tuple_test))


clean:
	$(RM) *.o *.mod *.a  *.inc *.F90 tuple_test



all:
html:
