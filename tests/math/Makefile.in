include @PFUNIT_DIR@/include/PFUNIT.mk
FFLAGS += $(PFUNIT_EXTRA_FFLAGS) -I@top_builddir@/src
FC = @FC@

%.o : %.F90
	$(FC) -c $(FFLAGS) $<


check: math_test


math_test_TESTS := math.pf
math_test_OTHER_LIBRARIES = -L@top_builddir@/src -lneko
$(eval $(call make_pfunit_test,math_test))


clean:
	$(RM) *.o *.mod *.a  *.inc *.F90 math_test



all:
html:
install:
