ifneq ("$(wildcard @PFUNIT_DIR@/include/PFUNIT.mk)", "")
include @PFUNIT_DIR@/include/PFUNIT.mk
endif
FFLAGS += $(PFUNIT_EXTRA_FFLAGS) -I@top_builddir@/src
FC = @FC@

%.o : %.F90
	$(FC) -c $(FFLAGS) $<


check: interpolation_test


interpolation_test_TESTS := interpolation_vertex.pf\
			interpolation_edge.pf\
			interpolation_quad.pf
interpolation_test_OTHER_LIBRARIES = -L@top_builddir@/src -lneko
$(eval $(call make_pfunit_test,interpolation_test))


clean:
	$(RM) *.o *.mod *.a  *.inc *.F90 interpolation_test



all:
html:
install:
distdir:
